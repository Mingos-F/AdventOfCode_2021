#pragma once
#include <vector>
#include <string>

class Functions {
public:
	int ConvertToDecimal(long long number);
	std::vector<std::string> SplitStr(std::string str, std::string deli);
	std::string ReturnString(char c);
private:
};